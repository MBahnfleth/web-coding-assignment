/**
 * Create a Marvel Developer Account
 * The value of your public key should be assigned to API_KEY. 
 * Don't worry; this file is git ignored and we will not know your API key.
 */

const API_KEY = '';

export default API_KEY;